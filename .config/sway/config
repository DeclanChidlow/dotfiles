### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4

# Your preferred programs
set $term alacritty
set $fileman pcmanfm-qt
set $menu bemenu-run -p Search -i -H 19 --fb "$black" --ff "$blue" --nb "$black" --nf "$white" --tb "$black" --hb "$black" --tf "$magenta" --hf "$magenta" --nf "$white" --af "$white" --ab "$black"

### Input configuration
#
    input type:touchpad {
        tap enabled
	middle_emulation enabled
	left_handed enabled
    }
    
# Colemak DH
	input type:keyboard {
	xkb_layout us
	xkb_variant colemak_dh
    }

### Key bindings
#
# Basics:
#
    # Start a predetermined programs
    bindsym $mod+Return exec $term
    bindsym $mod+Shift+Return exec $fileman

    # Kill focused window
    bindsym $mod+Shift+q kill

    # Start your launcher
    bindsym $mod+q exec $menu

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.

# Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Reload the configuration file
    bindsym $mod+Shift+c reload

    # Exit sway (logs you out of your Wayland session)
    bindsym $mod+Shift+e exec swaynag -t warning -m 'Are you sure you want to exit sway?' -B 'Yes!' 'swaymsg exit'
#
# Moving around:
#
    # Use $mod+[up|down|left|right]
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right
#
# Workspaces:
#
    # Switch to workspace
    bindsym $mod+1 workspace number 1
    bindsym $mod+2 workspace number 2
    bindsym $mod+3 workspace number 3
    bindsym $mod+4 workspace number 4
    bindsym $mod+5 workspace number 5
    bindsym $mod+6 workspace number 6
    bindsym $mod+7 workspace number 7
    bindsym $mod+8 workspace number 8
    bindsym $mod+9 workspace number 9
    bindsym $mod+0 workspace number 10
    # Move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace number 1
    bindsym $mod+Shift+2 move container to workspace number 2
    bindsym $mod+Shift+3 move container to workspace number 3
    bindsym $mod+Shift+4 move container to workspace number 4
    bindsym $mod+Shift+5 move container to workspace number 5
    bindsym $mod+Shift+6 move container to workspace number 6
    bindsym $mod+Shift+7 move container to workspace number 7
    bindsym $mod+Shift+8 move container to workspace number 8
    bindsym $mod+Shift+9 move container to workspace number 9
    bindsym $mod+Shift+0 move container to workspace number 10

#
# Layout stuff:
#
    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

#
# Resizing containers:
#
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

#
# Keyboard Control Buttons
#

# F1
bindsym --locked XF86AudioMute exec amixer set Master toggle
# F2
bindsym --locked XF86AudioLowerVolume exec amixer set Master 1%-
bindsym --locked Shift+XF86AudioLowerVolume exec amixer set Master 10%-
# F3
bindsym --locked XF86AudioRaiseVolume exec amixer set Master 1%+
bindsym --locked Shift+XF86AudioRaiseVolume exec amixer set Master 10%+
# F4
bindsym --locked XF86AudioPrev exec playerctl previous
# F5
bindsym --locked XF86AudioPlay exec playerctl play-pause
# F6
bindsym --locked XF86AudioNext exec playerctl next
# F7
bindsym --locked XF86MonBrightnessDown exec xbacklight -dec 5
bindsym --locked Shift+XF86MonBrightnessDown exec busctl --user call -- rs.wl-gammarelay / rs.wl.gammarelay UpdateTemperature n -100 
# F8
bindsym --locked XF86MonBrightnessUp exec xbacklight -inc 5
bindsym --locked Shift+XF86MonBrightnessUp exec busctl --user call rs.wl-gammarelay / rs.wl.gammarelay UpdateTemperature n 100
# F11
bindsym --locked Print exec sway-screenshot -o /home/vale/Pictures/screenshots -m output 
bindsym --locked Shift+Print exec sway-screenshot -o /home/vale/Pictures/screenshots -m region
# F12
bindsym --locked XF86AudioMedia exec $term -e btop

# Borders
hide_edge_borders smart_no_gaps

include /etc/sway/config.d/*

# colors
font pango:Lexend Deca Regular 9

set $black #1C1B19
set $bright_black #918175

set $red #EF2F27
set $bright_red #F75341

set $green #519F50
set $bright_green #98BC37

set $yellow #FBB829
set $bright_yellow #FED06E

set $blue #2C78BF
set $bright_blue #68A8E4

set $magenta #E02C6D
set $bright_magenta #FF5C8F

set $cyan #0AAEB3
set $bright_cyan #53FDE9

set $white #D0BFA1
set $bright_white #FCE8C3

set $orange #D75F00
set $bright_orange #FF8700

set $gray #4E4E4E
set $bright_gray #262626

set $hard_black #121212

# Colors                border        background  text          indicator child_border
client.focused          $black        $black      $yellow       $yellow   $black
client.focused_inactive $black        $black 	  $bright_black $gray     $black
client.unfocused        $black        $black	  $bright_black $gray     $black
client.urgent           $red          $black	  $red          $red      $red
client.background       $black

#
# Status Bar:
#
# Read `man 3 sway-bar` for more information about this section.
bar {
    position top
    status_command /usr/bin/i3status-rs /home/vale/.config/i3status-rust/config.toml

  colors {
    background $black
    statusline $white
    separator  $white

    # Colors           border        background   text
    focused_workspace  $black        $black      $yellow
    active_workspace   $black        $black      $bright_black
    inactive_workspace $black        $black      $bright_black
    urgent_workspace   $red          $red        $black
    binding_mode       $magenta      $magenta    $bright_white
  }
}

# Gnome Crap
exec_always {
  gsettings set org.gnome.desktop.interface document-font-name 'Lexend Deca Regular 9'
  gsettings set org.gnome.desktop.interface font-name 'Lexend Deca Regular 9'
  gsettings set org.gnome.desktop.interface monospace-font-name 'Lexend Deca Regular 9'
}

# Per Program Config
exec udiskie
exec librewolf
assign [app_id="librewolf"] workspace number 1
assign [title="Steam"] workspace number 6
assign [app_id="org.prismlauncher.PrismLauncher"] workspace number 6
assign [app_id="virt-manager"] workspace number 9
exec keepassxc
assign [app_id="org.keepassxc.KeePassXC"] workspace number 10
